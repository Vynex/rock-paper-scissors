import styles from '../../styles/Home.module.css';

const Header = () => {
	return (
		<div className={styles.header}>
			<div className={styles.banner}></div>
			<div className={styles.heading}>Rock Paper Scissors</div>
		</div>
	);
};

export default Header;
